\documentclass[tikz,border=6pt]{standalone}

\usepackage[utf8]{inputenc}
\usepackage[T5]{fontenc}
\usepackage[vietnamese]{babel}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,backgrounds,calc}

\definecolor{inputblue}{RGB}{218,235,255}
\definecolor{modelgreen}{RGB}{224,244,232}
\definecolor{vggtorange}{RGB}{255,235,210}
\definecolor{metricpurple}{RGB}{235,226,250}
\definecolor{resultgray}{RGB}{238,238,238}
\definecolor{linegray}{RGB}{80,80,80}

\tikzset{
  block/.style={
    rectangle,
    rounded corners=2pt,
    draw=linegray,
    line width=0.55pt,
    align=center,
    minimum width=34mm,
    minimum height=10mm,
    inner xsep=5pt,
    inner ysep=4pt,
    font=\small
  },
  smallblock/.style={
    block,
    minimum width=28mm,
    minimum height=8mm,
    font=\footnotesize
  },
  group/.style={
    rectangle,
    rounded corners=3pt,
    draw=linegray,
    line width=0.55pt,
    dashed,
    inner sep=7pt
  },
  arrow/.style={
    -{Latex[length=2.4mm,width=1.7mm]},
    draw=linegray,
    line width=0.65pt
  },
  labeltext/.style={
    font=\footnotesize,
    align=center,
    text=linegray
  }
}

\begin{document}
\begin{tikzpicture}[node distance=9mm and 13mm]

% Input
\node[block, fill=inputblue] (video) {Video đầu vào\\[-1pt]\footnotesize $I_1,\ldots,I_T$};

% Two branches
\node[block, fill=modelgreen, below left=15mm and 19mm of video] (depthmodel)
  {Mô hình ước lượng độ sâu\\[-1pt]\footnotesize VDA / Depth Anything};
\node[block, fill=vggtorange, below right=15mm and 19mm of video] (vggt)
  {VGGT cố định\\[-1pt]\footnotesize nguồn thông tin hình học};

\node[smallblock, fill=modelgreen, below=9mm of depthmodel] (depth)
  {Bản đồ độ sâu\\[-1pt]\footnotesize $D_t$};
\node[smallblock, fill=vggtorange, below=9mm of vggt] (geometry)
  {Camera, điểm theo dõi, độ tin cậy\\[-1pt]\footnotesize $K_t, P_t, u_{i,t}, c_{i,t}$};

% Optional controlled errors
\node[smallblock, fill=resultgray, below=9mm of depth] (errors)
  {Lỗi có kiểm soát\\[-1pt]\footnotesize trôi tỉ lệ, nhiễu, trễ khung hình};

% Metrics
\node[block, fill=metricpurple, below=25mm of $(errors)!0.5!(geometry)$] (metrics)
  {Đánh giá nhất quán hình học 3D};

\node[smallblock, fill=metricpurple, below left=9mm and -5mm of metrics] (m1)
  {Ổn định điểm 3D};
\node[smallblock, fill=metricpurple, below=9mm of metrics] (m2)
  {Sai số chiếu lại};
\node[smallblock, fill=metricpurple, below right=9mm and -5mm of metrics] (m3)
  {Trôi tỉ lệ};

% Results
\node[block, fill=resultgray, below=20mm of m2] (report)
  {Kết quả đánh giá\\[-1pt]\footnotesize bảng số liệu, biểu đồ, hình minh họa};
\node[block, fill=resultgray, right=14mm of report] (tta)
  {Hiệu chỉnh ở thời điểm kiểm thử\\[-1pt]\footnotesize chỉnh tỉ lệ / sửa nhỏ độ sâu};

% Arrows
\draw[arrow] (video) -- (depthmodel);
\draw[arrow] (video) -- (vggt);
\draw[arrow] (depthmodel) -- (depth);
\draw[arrow] (vggt) -- (geometry);
\draw[arrow] (depth) -- (errors);
\draw[arrow] (errors) |- (metrics);
\draw[arrow] (geometry) |- (metrics);
\draw[arrow] (metrics) -- (m1);
\draw[arrow] (metrics) -- (m2);
\draw[arrow] (metrics) -- (m3);
\draw[arrow] (m1) |- (report);
\draw[arrow] (m2) -- (report);
\draw[arrow] (m3) |- (report);
\draw[arrow] (report) -- (tta);

% Clarifying note
\node[
  labeltext,
  draw=linegray,
  rounded corners=2pt,
  fill=white,
  inner xsep=6pt,
  inner ysep=4pt,
  below=8mm of $(report)!0.5!(tta)$
] (note)
  {Ghi chú: không dùng VGGT làm độ sâu chuẩn; dùng thông tin hình học từ VGGT để kiểm tra nhất quán 3D.};

% Background groups
\begin{pgfonlayer}{background}
  \node[group, fit=(depthmodel)(depth)(errors), label={[labeltext]above:Mô hình cần đánh giá}] {};
  \node[group, fit=(vggt)(geometry), label={[labeltext]above:Thông tin hỗ trợ từ VGGT}] {};
  \node[group, fit=(metrics)(m1)(m2)(m3), label={[labeltext]above:Các thước đo đề xuất}] {};
\end{pgfonlayer}

\end{tikzpicture}
\end{document}
