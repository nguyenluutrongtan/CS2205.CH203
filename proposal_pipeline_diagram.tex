\documentclass[tikz,border=6pt]{standalone}

\usepackage[utf8]{inputenc}
\usepackage[T5]{fontenc}
\usepackage[vietnamese]{babel}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,backgrounds,calc,shadows,shadows.blur}

\definecolor{inputblue}{RGB}{218,235,255}
\definecolor{modelgreen}{RGB}{224,244,232}
\definecolor{vggtorange}{RGB}{255,235,210}
\definecolor{groundblue}{RGB}{220,238,250}
\definecolor{metricpurple}{RGB}{235,226,250}
\definecolor{resultgray}{RGB}{238,238,238}
\definecolor{linegray}{RGB}{80,80,80}

\tikzset{
  block/.style={
    rectangle,
    rounded corners=6pt,
    draw=linegray,
    line width=0.75pt,
    align=center,
    minimum width=34mm,
    minimum height=10mm,
    inner xsep=5pt,
    inner ysep=4pt,
    font=\small,
    blur shadow={
      shadow blur steps=5,
      shadow blur extra rounding=1.5pt,
      shadow xshift=1.2pt,
      shadow yshift=-1.2pt,
      shadow opacity=18
    }
  },
  smallblock/.style={
    block,
    minimum width=28mm,
    minimum height=8mm,
    font=\footnotesize
  },
  group/.style={
    rectangle,
    rounded corners=6pt,
    draw=linegray,
    line width=0.55pt,
    dashed,
    inner sep=7pt
  },
  arrow/.style={
    -{Latex[length=2.4mm,width=1.7mm]},
    draw=linegray,
    line width=0.65pt
  },
  labeltext/.style={
    font=\footnotesize,
    align=center,
    text=linegray
  },
  optional/.style={
    block,
    dashed
  }
}

\begin{document}
\begin{tikzpicture}[node distance=9mm and 13mm]

% Input
\node[block, fill=inputblue] (video) {Video đầu vào\\[-1pt]\footnotesize $I_1,\ldots,I_T$};

% Three branches
\node[block, fill=modelgreen, below left=15mm and 38mm of video] (depthmodel)
  {Mô hình ước lượng độ sâu\\[-1pt]\footnotesize VDA / Depth Anything};
\node[block, fill=vggtorange, below=15mm of video] (vggt)
  {VGGT cố định\\[-1pt]\footnotesize nguồn thông tin hình học};
\node[block, fill=groundblue, below right=15mm and 38mm of video] (groundtruth)
  {Dữ liệu ground truth\\[-1pt]\footnotesize chỉ dùng để kiểm chứng};

\node[smallblock, fill=modelgreen, below=9mm of depthmodel] (depth)
  {Bản đồ độ sâu\\[-1pt]\footnotesize $D_t$};
\node[smallblock, fill=vggtorange, below=9mm of vggt] (geometry)
  {Camera, điểm theo dõi, độ tin cậy\\[-1pt]\footnotesize $K_t, P_t, u_{i,t}, c_{i,t}$};
\node[smallblock, fill=groundblue, below=9mm of groundtruth] (reference)
  {Độ sâu, camera, tương ứng điểm\\[-1pt]\footnotesize dữ liệu tham chiếu độc lập};

% Optional controlled errors
\node[smallblock, fill=resultgray, below=9mm of depth] (errors)
  {Lỗi có kiểm soát\\[-1pt]\footnotesize trôi tỉ lệ, nhiễu, trễ khung hình};

% Evaluation protocols and metrics
\node[block, fill=metricpurple, below=25mm of geometry] (protocols)
  {Hai giao thức đánh giá\\[-1pt]\footnotesize VGGT và ground truth};

\node[smallblock, fill=metricpurple, below left=10mm and 7mm of protocols] (m1)
  {Ổn định điểm 3D};
\node[smallblock, fill=metricpurple, below=10mm of protocols] (m2)
  {Chiếu lại dài hạn};
\node[smallblock, fill=metricpurple, below right=10mm and 7mm of protocols] (m3)
  {Trôi tỉ lệ--độ lệch};

% Results
\node[block, fill=resultgray, below=20mm of m2] (report)
  {Kết quả đánh giá\\[-1pt]\footnotesize bảng số liệu, biểu đồ, hình minh họa};
\node[optional, fill=resultgray, right=14mm of report] (tta)
  {Thử nghiệm hiệu chỉnh\\[-1pt]\footnotesize affine theo thời gian};

% Arrows
\draw[arrow] (video) -- (depthmodel);
\draw[arrow] (video) -- (vggt);
\draw[arrow] (video) -- (groundtruth);
\draw[arrow] (depthmodel) -- (depth);
\draw[arrow] (vggt) -- (geometry);
\draw[arrow] (groundtruth) -- (reference);
\draw[arrow] (depth) -- (errors);
\draw[arrow] (errors) |- (protocols);
\draw[arrow] (geometry) -- (protocols);
\draw[arrow] (reference) |- (protocols);
\draw[arrow] (protocols) -- (m1);
\draw[arrow] (protocols) -- (m2);
\draw[arrow] (protocols) -- (m3);
\draw[arrow] (m1) |- (report);
\draw[arrow] (m2) -- (report);
\draw[arrow] (m3) |- (report);
\draw[arrow, dashed] (m3) |- (tta);
\draw[arrow, dashed] (tta) -- (report);

% Clarifying note
\node[
  labeltext,
  draw=linegray,
  rounded corners=5pt,
  fill=white,
  inner xsep=6pt,
  inner ysep=4pt,
  blur shadow={
    shadow blur steps=4,
    shadow blur extra rounding=1.2pt,
    shadow xshift=1pt,
    shadow yshift=-1pt,
    shadow opacity=14
  },
  below=8mm of $(report)!0.5!(tta)$
] (note)
  {Ground truth chỉ dùng để đối chứng; nhánh hiệu chỉnh chỉ dùng tín hiệu VGGT và không cập nhật trọng số mô hình.};

% Background groups
\begin{pgfonlayer}{background}
  \node[group, fit=(depthmodel)(depth)(errors), label={[labeltext]above:Mô hình cần đánh giá}] {};
  \node[group, fit=(vggt)(geometry), label={[labeltext]above:Thông tin hỗ trợ từ VGGT}] {};
  \node[group, fit=(groundtruth)(reference), label={[labeltext]above:Nhánh đối chứng độc lập}] {};
  \node[group, fit=(protocols)(m1)(m2)(m3), label={[labeltext]above:Các chỉ số đánh giá}] {};
\end{pgfonlayer}

\end{tikzpicture}
\end{document}

